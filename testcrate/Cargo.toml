[package]
name = "testcrate"
version = "0.0.0"
edition = "2024"
publish = false

# Note: we put the example binaries as normal binaries and not examples
# because it changes the locations in target directories from what is usual

[dependencies]
clap = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
stacked_errors = { workspace = true }
super_orchestrator = { workspace = true, features = ["bollard"] }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
